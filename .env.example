# Example environment variables (do NOT include secrets)
# Copy to .env and fill values locally or from your secret manager

# Application
APP_ENV=development
APP_PORT=3000

# Example token names (do not place values here)
VAULT_TOKEN=
API_TOKEN=

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agi_vault_db
DB_USER=agi_vault_user
DB_PASSWORD=

# Add other variables used by components here
